---
name: "{fmt}"
slug: fmtlib
language: cpp
description: Fast, safe, and expressive string formatting library for C++ — the basis for std::format in C++20.
website: https://fmt.dev
github: fmtformat/fmt
year: 2012
pricing: free
open_source: true
license: MIT
library_type: utility
tags: [formatting, strings, cpp, modern-cpp, output, logging]
related_libraries: [nlohmann-json]
---

`{fmt}` is significantly faster than `printf` and `std::cout` — its compile-time format string checking catches errors before runtime. It became the blueprint for `std::format` in C++20 and supports custom formatters for user-defined types. Used in spdlog, Qt, and many systems-level projects.
